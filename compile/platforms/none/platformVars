PLATFORM_DIR = platforms

all:
	@echo "No PLATFORM specified, please specify a PLATFORM"
	@echo
	@echo "Current PLATFORMs are:"
	@cd ${PLATFORM_DIR} ; \
		for p in * ; do \
			if [ $$p != "none" ] ; then \
				echo "    PLATFORM=$$p" ; \
			fi \
		done
